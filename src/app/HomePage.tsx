import { HeroNav } from "~/styles/components/navbar";

export const HomePage = () => {
    return (
        <div
            className="w-full min-h-screen ">
            <HeroNav />
            <HeroFooter />
        </div>

    );
};

