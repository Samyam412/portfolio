import { HeroNav } from "~/styles/components/navbar";

const HomePage = () => {
    return (
        <div
            className="w-full h-full " >
            <HeroNav />
        </div>

    );
};

export default HomePage;
